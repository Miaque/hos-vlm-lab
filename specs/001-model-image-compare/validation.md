# 实现与验收记录

更新日期：2026-09-11。结论：本地工作台实现及模拟验收完成；真实提供方取证和真实调用验收未完成。任务状态以 [tasks.md](tasks.md) 为准。

## 自动验证

本次收尾重新执行：

| 检查 | 结果 |
| --- | --- |
| `uv run pytest -q` | 62 passed，2 个依赖弃用警告 |
| `uv build` | wheel 与源码分发包构建成功 |

本会话此前还执行了 `uv sync --frozen`、`uvx ruff check --select F src tests`、`node --check src/hos_vlm_lab/static/app.js`，均通过。检查 wheel 包含默认提示词和三个静态页面资源，且不包含 tests 包。上述检查未发起真实模型请求。

两个警告来自 Starlette TestClient 对 HTTPX 及 AnyIO 兼容接口的弃用提示；未屏蔽警告，当前测试通过。不将测试通过视为真实服务连通性或计费证明。

| 任务 | 实现与验证依据 |
| --- | --- |
| T001–T003 | pyproject/uv.lock、忽略规则与环境示例、tests/fakes.py 与 conftest.py；测试使用独立目录，禁止真实出站连接，允许 Windows asyncio 本机唤醒连接 |
| T004–T007 | test_contracts、test_controls、test_store、test_api；严格输入/事件协议、未知能力拒绝、SQLite 外键与原子提交、请求去重、生命周期关闭、Host/Origin 限制及安全错误结构 |
| T008–T012 | test_images、test_default_prompt、test_gateway、test_runner、test_api；EXIF/透明背景/缩放、格式和数量限制、整批失败、默认种子身份、首次响应/超时/429/截断、四路并发和慢模型隔离、事务失败零调用 |
| T015–T017 | test_controls、test_contracts、test_prompt_snapshot；预算开关、非法数值、参数边界、不兼容模型导致整轮零调用、原样文本及两轮不可变快照。部分验证按实际模块分布，不限于任务最初建议的单个测试文件 |
| T020–T021 | test_history、test_store、test_api；分页、源文件移动后仍可读、重启中断、不自动调度、缺失记录和参数边界 |
| T024–T028 | test_stop_retry、test_usage_cost、test_runner、test_gateway、test_api；停止认领竞争、读取图片期间停止、独立重试批次、重试去重及身份变化拒绝、重试再次停止、存储失败停止调度、Decimal 计算、未知与零值、思考不重复计费、失败响应保留用量 |
| T031 | test_api、test_gateway 及浏览器文本检查；密钥与 JSON 转义密钥脱敏、任意路径拒绝、安全存储错误、测试无默认付费调用；测试和打包通过 |
| T032 | README、.env.example、AGENTS.md、quickstart.md 已同步启动/测试命令、实现结构、价格区间及真实接入限制 |

## 浏览器操作证据

以下为本会话在 2026-09-11 已执行的真实浏览器操作，收尾时汇总入档，未将其写成重新执行。浏览器访问本机 `127.0.0.1:8001`，入口为 `tests.browser_app`，持久目录 `.test-data/browser`。所有模型均为明确标注的模拟网关，样本为本地生成的图形测试图片。

### US1：同图多模型（T013–T014）

- 上传 sample1.png 和 sample2.png，均显示实际处理尺寸 640×360。
- 使用完整 JSON `{"events":[{"code":"person","name":"人员"}]}`，选择四个模拟模型，创建轮次 `347aed69…`（本地时间 14:35:15）。
- 浏览器先观察到进行中/等待中，结束后为 6 个成功、2 个调用失败，共 8 个独立组合。切换第二张图片仍显示对应四个模型。
- 检出事件、有效空 events 的“未检出（不代表确认不存在）”和“模拟调用失败”分别显示。展开详情读取首次响应、参数与用量；缺失值显示未知。
- 页面带模拟模式横幅，模型名称带“模拟”，无伪造真实识别结论。此结果不关闭 SC-001 的真实三图四模型条件。

### US2：提示词与参数（T018–T019）

- 从历史复用输入，将场景名改为“人员 v2”，开启思考、预算填 0，浏览器收到明确 422 输入校验错误，没有创建新轮。
- 使用 thinking=true、thinking_budget=1024、max_tokens=4096、temperature=0.6 成功创建模拟轮次 `0bbbfd17…`（14:43:10）。
- 详情显示四项实际参数；历史旧轮仍保留原“人员”输入，新轮显示“人员 v2”。逐字发送及编辑隔离另由自动测试验证。
- 点击恢复默认后，编辑器重新包含来源标识 `755f31a`；已有历史结果不变。配置响应无测试密钥，脱敏由自动测试补充验证。

### US3：历史与对照（T022–T023）

- 重启模拟服务，浏览器从历史恢复轮次及图片。
- 将测试源文件 sample1.png 移为 sample1-moved.png，再重启服务；历史中原 sample1.png 的预览仍成功加载，naturalWidth=640、naturalHeight=360，实际发送哈希前缀仍为 `bfeccdcfc24e7326`。
- A/B 对照 `0bbbfd17…` 与 `347aed69…`，两张图片分别对应，失败和重试均保留；第二张图的已停止状态没有变成未检出。
- 创建只包含一个模型的新轮 `6550c9a8…`（14:55:03），与四模型旧轮对照，浏览器检查到 6 个“未运行”标记，符合两张图各缺三模型。
- 页面实现按图片身份优先、原图哈希补充的一对一匹配，并提示处理哈希差异。历史接口分页有自动测试；分页按钮与预处理差异提示代码已实现，未单独制造百轮或不同预处理版本的浏览器样本。

### US4：停止、重试与费用（T029）

- 第二轮开始后立即停止，浏览器显示“停止中”，4 个已在途、4 个未开始已停止；已发出项继续完成。
- 结束后手动重试失败项，页面显示新的“尝试 2”和“检测中”，旧 4 个 stopped 不会让重试显示 stopping。重试结束后旧失败与新失败均保留。
- 页面显示实际模拟调用耗时，例如 1016ms 与慢模型 4015ms；token/费用缺失显示未知。真实费用未验证；已知用量与价格的 Decimal 结果由自动测试验证。

### 页面文本安全

- 模拟轮次使用事件编码 `<img src=x onerror=alert(1)>`，展开结果原文。
- 浏览器显示该文本，结果区注入的 img 数量为 0，没有脚本对话框；控制台 error 日志为空。
- 检查了桌面页面布局。未执行移动设备完整交互验收。

## 未完成与外部条件

- **T030 未完成**：实际提供方、地域及完整端点未明确，Qwen 两款及 Kimi 模型专属上限仍缺证据。现有 DeepSeek 官方非思考 profile 有文档范围校验，不代表账号连通性已经验证。来源与限制见 [model-compatibility.md](model-compatibility.md)。
- **T033 未完成**：未执行真实单图单模型，也未执行真实三图四模型。此前仅检查配置齐全与否，四组均为 false，没有输出或复制密钥。后续需合法环境配置、T030 证据及明确真实调用范围。

本地完成项不包含准确率/召回率报表、画框、多图联合推理或生产部署。没有 Git 提交或推送。`.specify/extensions.yml` 不存在，无需执行实现后扩展钩子。

## 2026-09-11 新增独立 27B 槽位验证

T034：先运行新测试，复现缺少第五个槽位及五模型请求被拒绝（3 failed、3 passed）；修改后全套 `uv run pytest -q` 为 **64 passed，2 个原有依赖弃用警告**，`uv build` 成功。验证独立 BASE_URL 尾斜杠处理、API_KEY 不回退、不进入身份快照、未取证 profile 拒绝，以及五模型两图共 10 次模拟调用、每模型单在途和慢模型隔离。界面由配置列表动态生成选项，本次未重新执行浏览器验收。

未执行真实模型请求；27B 实际服务商和地址待用户提供，T030/T033 保持未完成。

## 原四模型共享连接验证

T035：新增测试先得到 2 failed、2 passed，修改后全套 `uv run pytest -q` 为 66 passed、2 个原有依赖弃用警告；`uv build` 成功。验证共享 BASE_URL 尾斜杠拼接、四模型共用密钥且保留各自 ID/profile、旧逐模型连接不再读取、共享配置缺失不回退，以及 27B 密钥独立。未执行真实调用；T030/T033 仍待实际端点取证。
