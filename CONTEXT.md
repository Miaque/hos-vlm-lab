# 多模型图片对比调试

在相同图片、提示词和可比参数下观察多个模型的检测效果，通过微调提示词并对照历次结果进行调试。

## Language

**图片**：一次独立检测的视觉输入。选择多张图片时，每张分别检测，不将多张合并为一次综合判断。

**图片组**：用户选取的一组待检测图片，可在修改提示词后复用于后续对比轮次。

**对比轮次**：使用一组图片、选定模型、共同提示词和可比参数进行的一轮检测。每轮保留当时的提示词、参数和结果，供后续对照。

**检测结果**：某一轮中某个模型对某张图片的判断、原因和原始输出。第一版不包含目标框，结果按图片与模型对应展示。
_Avoid_: 人工真值、准确率

**未检出**：模型的有效响应未包含某个待检测事件，可能表示事件不存在或可见证据不足。调用失败和格式错误不属于未检出。
_Avoid_: 确定不存在、正常

**可见证据（evidence）**：模型返回的目标位置、类型、可见特征或行为描述，用于说明检出事件的图像依据。

**置信度（confidence）**：模型对检出事件给出的 0–1 分值，仅作为辅助信息，不等同于实测准确率，也不直接用于跨模型排名。

**可比参数**：参与对比的模型共同支持且取值一致的推理参数。不支持的参数需明确显示，不能静默忽略后视为条件一致。

**提示词微调**：编辑提供给模型的提示词，再执行新一轮检测并对照前后结果。
_Avoid_: 模型微调、模型训练

**共用提示词**：同一轮中提供给所有选定模型的相同提示词，可同时描述多个待检测场景。
