生成 Git commit message 时，使用 Conventional Commits + Commit Body 格式，并严格基于当前提交的实际改动内容生成。

格式：

<type>: <简洁的中文标题>

<1～3 行中文正文，说明本次提交的主要改动>

要求：

* 第一行为 Conventional Commits 格式：
  `<type>: <description>`
* type 使用英文小写，例如：
  `feat`、`fix`、`refactor`、`perf`、`test`、`docs`、`chore`、`build`、`ci`
* 根据实际改动选择最准确的 type
* type 后使用英文冒号 `:`，冒号后保留一个空格
* 标题和正文均使用简体中文
* 标题使用简洁的动宾结构，概括本次提交的核心改动
* 标题不添加句号
* 标题与正文之间保留一个空行
* 正文使用自然、完整的中文句子
* 正文说明实际完成的主要改动、关键行为变化或测试覆盖
* 正文通常控制在 1～3 行
* 多项相关改动应按逻辑归纳，不逐文件罗列
* 不使用 Markdown 列表
* 不使用 emoji
* 不添加无实际信息的描述
* 不夸大、不推测、不补充 Git diff 中无法确认的内容
* 术语、模块名、类名、路径等按项目中的实际写法保留
* 生成前先分析当前 staged diff，只描述本次实际提交的内容

示例：

feat: 增加巡逻机器人运动补偿抽帧

为移动摄像头补偿全局平移，并增加兜底抽帧、规则版本和周期统计。

test: 添加多模态 LLM 单元测试、集成测试及测试资源

单元测试覆盖 Client 请求构造、图片缩放、Processor 分析和告警推送。
集成测试使用 tests/images 中的真实监控画面调用线上 LLM 配置。

fix: 修复视频流断线后抽帧任务未恢复

补充视频流重连后的任务状态恢复逻辑，并避免重复创建抽帧任务。
增加对应异常场景测试。

refactor: 重构多模态模型调用链路

拆分模型 Client、图片预处理和结果解析职责，统一模型调用接口。
调整相关调用关系和测试。
